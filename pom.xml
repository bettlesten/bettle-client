<?xml version="1.0" encoding="UTF-8"?>
<!--
  ============================================================================
   Bettle Client - Launcher Build Configuration
  ============================================================================
   NOTE ON FABRIC LOOM:
   Fabric Loom is a *Gradle* plugin and has no Maven equivalent. The Fabric
   client mod therefore lives in the sibling module `bettle-mod/` and is built
   with Gradle + Loom (see bettle-mod/build.gradle). This POM builds the
   JavaFX launcher only. `mvn package` produces a runnable fat jar.
  ============================================================================
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>net.bettleclient</groupId>
    <artifactId>bettle-client</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>Bettle Client</name>
    <description>Crimson-themed Minecraft launcher and client framework with dynamic version management</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <java.version>17</java.version>
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>
        <maven.compiler.release>17</maven.compiler.release>

        <javafx.version>21.0.4</javafx.version>
        <gson.version>2.11.0</gson.version>
        <minecraftauth.version>4.1.1</minecraftauth.version>
        <commons.lang3.version>3.14.0</commons.lang3.version>
        <commons.io.version>2.16.1</commons.io.version>
        <commons.compress.version>1.26.2</commons.compress.version>
        <slf4j.version>2.0.13</slf4j.version>

        <main.class>net.bettleclient.Main</main.class>
        <launcher.class>net.bettleclient.ui.BettleUI</launcher.class>

        <!-- 'compile' shades JavaFX into the fat jar so `java -jar` works on any
             JDK 17+. The `jpackage` profile flips this to 'provided' so JavaFX
             comes from the bundled runtime image instead (see packaging/windows). -->
        <javafx.scope>compile</javafx.scope>
    </properties>

    <repositories>
        <repository>
            <id>central</id>
            <url>https://repo1.maven.org/maven2/</url>
        </repository>
        <!-- RaphiMC / Lenni0451 host MinecraftAuth and its httpclient dependency here. -->
        <repository>
            <id>lenni0451-releases</id>
            <url>https://maven.lenni0451.net/releases</url>
        </repository>
        <repository>
            <id>lenni0451-snapshots</id>
            <url>https://maven.lenni0451.net/snapshots</url>
        </repository>
        <repository>
            <id>opencollab-releases</id>
            <url>https://repo.opencollab.dev/maven-releases/</url>
        </repository>
    </repositories>

    <dependencies>

        <!-- ==================== JavaFX 21 (modern UI) ==================== -->
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-base</artifactId>
            <version>${javafx.version}</version>
            <scope>${javafx.scope}</scope>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-graphics</artifactId>
            <version>${javafx.version}</version>
            <scope>${javafx.scope}</scope>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
            <version>${javafx.version}</version>
            <scope>${javafx.scope}</scope>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-fxml</artifactId>
            <version>${javafx.version}</version>
            <scope>${javafx.scope}</scope>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-swing</artifactId>
            <version>${javafx.version}</version>
            <scope>${javafx.scope}</scope>
        </dependency>

        <!-- ==================== JSON ==================== -->
        <dependency>
            <groupId>com.google.code.gson</groupId>
            <artifactId>gson</artifactId>
            <version>${gson.version}</version>
        </dependency>

        <!-- =========== Microsoft OAuth2 / Minecraft session auth =========== -->
        <dependency>
            <groupId>net.raphimc</groupId>
            <artifactId>MinecraftAuth</artifactId>
            <version>${minecraftauth.version}</version>
        </dependency>

        <!-- ==================== Apache Commons ==================== -->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
            <version>${commons.lang3.version}</version>
        </dependency>
        <dependency>
            <groupId>commons-io</groupId>
            <artifactId>commons-io</artifactId>
            <version>${commons.io.version}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-compress</artifactId>
            <version>${commons.compress.version}</version>
        </dependency>

        <!-- ==================== Logging ==================== -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4j.version}</version>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-simple</artifactId>
            <version>${slf4j.version}</version>
        </dependency>

    </dependencies>

    <build>
        <finalName>BettleClient-${project.version}</finalName>

        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>false</filtering>
            </resource>
        </resources>

        <plugins>

            <!-- ==================== Compiler ==================== -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>${java.version}</release>
                    <encoding>UTF-8</encoding>
                    <compilerArgs>
                        <arg>-Xlint:-options</arg>
                    </compilerArgs>
                </configuration>
            </plugin>

            <!-- ============ Run from source: mvn javafx:run ============ -->
            <plugin>
                <groupId>org.openjfx</groupId>
                <artifactId>javafx-maven-plugin</artifactId>
                <version>0.0.8</version>
                <configuration>
                    <mainClass>${launcher.class}</mainClass>
                    <options>
                        <option>-Dprism.lcdtext=false</option>
                        <option>-Dfile.encoding=UTF-8</option>
                    </options>
                </configuration>
            </plugin>

            <!-- ======== Fat jar. Main-Class is the non-Application ========
                 bootstrap (net.bettleclient.Main) so that a shaded jar can be
                 started with `java -jar` without the JavaFX module path. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.3</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <shadedArtifactAttached>false</shadedArtifactAttached>
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>module-info.class</exclude>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>${main.class}</mainClass>
                                </transformer>
                            </transformers>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- ==================== Plain jar manifest ==================== -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.1</version>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>${main.class}</mainClass>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>

        </plugins>
    </build>

    <profiles>
        <!-- ================================================================
             mvn -P jpackage clean package
             Produces a jar WITHOUT JavaFX bundled, for use with jpackage and a
             Full JDK (Liberica Full / Zulu FX) whose runtime image already
             carries the javafx.* modules. Everything else is still shaded, so
             the result is a single input jar.
             ================================================================ -->
        <profile>
            <id>jpackage</id>
            <properties>
                <javafx.scope>provided</javafx.scope>
            </properties>
        </profile>
    </profiles>
</project>
